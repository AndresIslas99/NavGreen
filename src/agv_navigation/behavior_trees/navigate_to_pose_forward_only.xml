<!--
  Forward-only navigation BT for AGV greenhouse robot.

  Hardware constraint: the robot has ONLY a front-facing ZED 2i camera and the
  collision_monitor stop/slowdown polygons extend almost exclusively forward
  (front=0.55m, rear=-0.35m). There is NO rear obstacle perception.

  Rationale for the changes vs Nav2 default
  (navigate_to_pose_w_replanning_and_recovery.xml):
   - The default RoundRobin recovery cycles through ClearingActions → Spin →
     Wait → BackUp. The BackUp action is REMOVED here because it commands the
     robot to move 30 cm in reverse — directly into a blind spot with no
     collision protection.
   - The replacement RoundRobin keeps ClearingActions, Spin, and Wait. If the
     robot still cannot find a forward trajectory after these recoveries, the
     BT aborts loud (reports failure to the action client) instead of
     silently driving backward.
   - Aborting is the safer fail mode: the operator sees the failure on the
     dashboard and can manually intervene with teleop, instead of the robot
     reversing into an unseen obstacle.

  Motion smoothness changes (2026-04-12):
   - RateController raised from 1.0 Hz to 2.0 Hz. At 0.25 m/s this means the
     robot operates on plans at most 12.5 cm stale instead of 25 cm stale,
     which reduces the "searching" behaviour on straight-line goals.

  SmoothPath removal (2026-04-13 Fase 7 F5):
   - Previously we inserted a SmoothPath action between ComputePathToPose
     and FollowPath to convert the grid-aligned SmacPlanner2D output into a
     curvature-continuous path. During the post-Fase6 navigation trip the
     BT logged `Timed out while waiting for action server to acknowledge
     goal request for smooth_path` — the smoother_server could not ACK its
     action goals under CPU pressure (compounded by nvblox + slam_web_gui
     eating >2 cores, removed in F1+F2 of the same phase).
   - SmoothPath was removed rather than throttled because:
     1) MPPI already has PreferForwardCritic + PathAlignCritic critics
        that produce smooth forward trajectories from grid-aligned inputs,
        so the smoother was marginal quality gain.
     2) Removing it eliminates one action-server protocol round-trip per
        replan (2 Hz × extra overhead per call), freeing additional CPU.
     3) If path smoothness becomes an issue in the field, re-introduce
        SmoothPath in a dedicated <Sequence> OUTSIDE the PipelineSequence
        hot path per Nav2 tutorial best practice:
        https://docs.nav2.org/tutorials/docs/adding_smoother.html
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <RateController hz="2.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath path="{path}" controller_id="FollowPath"/>
          <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>
      </PipelineSequence>
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <Sequence name="ClearingActions">
            <ClearEntireCostmap name="ClearLocalCostmap-Subtree" service_name="local_costmap/clear_entirely_local_costmap"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Subtree" service_name="global_costmap/clear_entirely_global_costmap"/>
          </Sequence>
          <Spin spin_dist="1.57"/>
          <Wait wait_duration="5"/>
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
